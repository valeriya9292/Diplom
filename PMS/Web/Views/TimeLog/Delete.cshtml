<script type="text/html" id="ko-deleteForm">
    <h3>Are you sure you want to delete this?</h3>
    <div class="css-link">
        <a href="#" data-bind="event: { click: function(){$parent.isDeleteOpen(false);}}">Back</a>

    </div>
    <fieldset id="deleteForm">
        <legend>Timelog</legend>
        <form>
            <p>
                <b>Title:</b>
                <span data-bind="text:title"></span>
            </p>
            <p>
                <input type="submit" value="Delete" class="formbutton" data-bind="click: function(){$parent.deleteTimelog({id: id}); return false;}" />
            </p>
        </form>
    </fieldset>

</script>